# Tests

This directory is reserved for:

- YAML linting;
- Ansible syntax checks;
- role contract tests;
- Molecule scenarios;
- FreeBSD integration tests.

The initial skeleton includes only a structure test.
