# Shared templates

Prefer role-local templates under:

```text
roles/<role>/templates/
```

Use this directory only for templates genuinely shared across multiple roles.

Every complete text file managed by Ansible should contain clear start and end
ownership comments whenever its format permits comments.
