# base

## Purpose

Common configuration applied to every managed FreeBSD host.

## Current state

Skeleton only. The role performs no real host changes.

## Host changes

None until the placeholder tasks are replaced.

## Structure

```text
defaults/main.yml
handlers/main.yml
tasks/main.yml
templates/
files/
```
